# 智数AI助手 项目协作规范

## 本地优先原则

- 本项目生成或修改的所有前端代码、页面、素材和演示数据默认仅用于本地开发与调试。
- 完成前端工作后，只启动本地开发服务器并提供 `localhost` 预览；不要自动发布到任何远程站点。
- 除非用户在当前任务中明确要求发布，否则禁止创建、保存、部署、更新或公开任何站点，也禁止调用站点托管、部署及访问权限变更工具。
- 不要因为用户说“完成”“生成”“上线效果”或“给我看看”而推断发布授权；这些表述默认仍指本地实现与本地预览。

## 工程结构

- 前端 `apps/web/`：React + Vite + Univer 表格。
- 后端 `apps/api/`：NestJS + Prisma + PostgreSQL，OpenCode 运行时网关。
- 分析 Agent Runtime `services/excel-agent/`：`opencode serve` 容器 + vendored `huashu-excel` skill。
- 所有真实的 Excel 分析能力由 `huashu-excel` skill 提供，后端**不重写**分析逻辑。

## 工作方式

- 开发与调试优先使用现有项目脚本和本地开发服务器。
- 未经用户明确要求，不新增后端服务、数据库或线上依赖。
- 所有面向用户的 AI 文字必须是简体中文。
